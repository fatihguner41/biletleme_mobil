class ImageEntity {
  final String ratio;
  final String url;
  final int width;
  final int height;

  ImageEntity({required this.ratio,required this.url,required this.width,required this.height});
}