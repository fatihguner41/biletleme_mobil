name: ticketing
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5

  # Network - STABİL VERSİYONLAR
  dio: ^5.3.3
  json_annotation: ^4.9.0
  # DI
  get_it: ^7.7.0

  # Utils
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  # Code generation - PUB.DEV'DE MEVCUT VERSİYONLAR
  build_runner: ^2.4.8
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
  assets:
    - assets/icons/app_icon.png